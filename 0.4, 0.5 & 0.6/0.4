sequenceDiagram
    participant User
    participant Browser
    participant Server

    Note over Browser, User: User writes a note and clicks 'Save'
    Browser->>Browser: Validate note
    Browser->>Browser: Create JSON object
    Browser->>Server: POST new note
    activate Server
    Server-->>Browser: 200 OK (Note saved)
    deactivate Server
    Browser->>Browser: Clear input field, Update UI
